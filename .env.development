# Loaded only by `next dev` (local development) — NOT by production builds.
# The CSP middleware enforces by default; keep it report-only locally so
# enforcement doesn't block HMR, eval-based React Refresh, or the error overlay.
CSP_MODE=report-only

# Test Markdown Dataset

Fixture used by the Dataset Management journey to verify `.md` file ingestion (issue #1117).

## Supported file types

- `.pdf`, `.docx`, `.pptx`, `.xlsx`, `.csv`, `.txt`
- `.md` / `.markdown` — added in this change

## Seeded fact

The default chat area width is **800 px**.

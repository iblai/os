// jest-dom's matchers (toBeInTheDocument, toHaveClass, …) on vitest's expect,
// for this tsconfig; the runtime registration is the root __tests__/vitest.setup.ts.
import '@testing-library/jest-dom/vitest';
